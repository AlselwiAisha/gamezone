import React from "react";
import { StyleSheet, View, Text } from "react-native";

export default function ReviwDetails() {
    return (
        <View style={styles.container}>
        <Text>Reviw Details Screen</Text>
        </View>
    );
    }

const styles = StyleSheet.create({
    container: {
        padding: 24,
    },
});